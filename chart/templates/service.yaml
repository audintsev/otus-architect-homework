apiVersion: v1
kind: Service
metadata:
  name: {{ include "homework6-chart.fullname" . }}
  labels:
  {{- include "homework6-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
  {{- include "homework6-chart.selectorLabels" . | nindent 4 }}
