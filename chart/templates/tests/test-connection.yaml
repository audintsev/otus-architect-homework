apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homework6-chart.fullname" . }}-test-connection"
  labels:
    {{- include "homework6-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homework6-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
